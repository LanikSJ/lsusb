# Cline Rules for lsusb Repository

## Repository Overview

lsusb provides enhanced USB device listing and information utilities.

## Code Standards and Practices

### Code Quality

- Write clear, maintainable code in the appropriate language
- Implement proper error handling and validation
- Include comprehensive help and usage information
- Test functionality across different systems and USB devices

### Documentation Standards

- Include clear installation and usage instructions
- Document command-line options and output formats
- Provide troubleshooting guidance for common USB issues
- Use markdown formatting consistently

### Markdown Compliance Requirements

- **ALL markdown files (.md) MUST pass markdownlint validation with zero errors or warnings**
- Run `markdownlint <filename>` on every markdown file before considering it complete
- Follow the project's `.markdownlint.json` configuration strictly
- Address ALL markdownlint issues immediately - no exceptions or workarounds
- Common requirements include:
  - Maximum line length of 80 characters (MD013)
  - Consistent heading styles and hierarchy
  - Proper list formatting and indentation
  - Blank lines around headings and code blocks
  - Consistent link and reference formatting
  - No trailing whitespace
  - Files must end with newlines
  - Proper table formatting when applicable
- Use `markdownlint --fix <filename>` for auto-fixable issues when available
- Validate markdown files in CI/CD pipelines where applicable

## Development Guidelines

### When Making Changes

- Preserve existing functionality unless explicitly asked to change it
- Update documentation when adding new features or modifying output
- Test changes across different Linux distributions and hardware
- **Always run markdownlint and fix all issues in markdown files before considering changes complete**

### System Tool Standards

- Handle various USB device types and configurations
- Provide both human-readable and machine-readable output formats
- Implement proper permissions handling for USB device access
- Test compatibility across different Linux kernel versions
