name: LSUSB Self Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  lsusb-test:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Test LSUSB
        run: ./lsusb
