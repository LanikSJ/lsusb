name: lsusb-self-test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  lsusb-test:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Test LSUSB
        run: ./lsusb
